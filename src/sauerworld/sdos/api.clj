(ns sauerworld.sdos.api)

(def api-queue
  "queue/storage")

(defn start-api
  [& [queue]]
  ;; no-op
  nil)

(defn request
  "Performs storage api requests. Throws Throwable on error."
  [action & params]
  ;; no-op
  nil)
